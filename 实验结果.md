# 1

## config

```
"""LCRE-CFNet coarse-fine baseline configuration."""

from __future__ import annotations

from pathlib import Path


# =========================
# Paths
# =========================

SCRIPT_DIR = Path(__file__).resolve().parent

DEFAULT_TRAIN_CSV = "../../dataset/all_features/train_val/train.csv"
DEFAULT_TEST_CSV = "../../dataset/all_features/train_val/test.csv"


# =========================
# Experiment
# =========================

EXPERIMENT_NAME = "LCRE-CFNet_13_CoarseFine_Consistency_LAMBDA3.0_C1.0"
RANDOM_SEED = 42


# =========================
# Data
# =========================

FEATURES = [
    "GR",
    "SP",
    "AC",
    "CAL",
    "DEN1",
    "CNL1",
    "PE",
    "log（RD）",
    "SW",
    "RD_RS_ratio",
]

# FEATURES = [
#     "GR",
#     "SP",
#     "AC",
#     # "AC1",
#     "CAL",
#     "U",
#     "DEN",
#     # "DEN1",
#     "CNL",
#     # "CNL1",
#     "PE",
#     "RD",
#     "RS",
#     # "log（RD）",
#     # "log（RS）",
#     # "? Φ1",
#     # "? Φ2",
#     # "? Φ3",
# # 新增黄金物理特征
# #     "RWA",
# #     "VSH_GR",
# #     "PHI_DENSITY",
# #     "EXCAVATION_DIFF",
# #     "INVASION_DIFF",
# ]

LABEL_CANDIDATES = ["试油结论", "y_fine"]

MISSING_VALUES = [
    -9999,
    "-9999",
    "#REF!",
    "",
    " ",
    "NA",
    "N/A",
    "nan",
    "NaN",
    "None",
]

CLASS_NAMES = ["气层", "含气层", "含气水层", "气水同层", "水层", "干层"]
NUM_CLASSES = len(CLASS_NAMES)

COARSE_CLASS_NAMES = ["气层优势类", "气水风险类", "非气层类"]
NUM_COARSE_CLASSES = len(COARSE_CLASS_NAMES)

COARSE_MAPPING = {
    "气层": "气层优势类",
    "含气层": "气层优势类",
    "含气水层": "气水风险类",
    "气水同层": "气水风险类",
    "水层": "气水风险类",
    "干层": "非气层类",
}


# =========================
# Model
# =========================

HIDDEN_DIMS = [100, 100, 50]
DROPOUT = 0.5


# =========================
# Training
# =========================

EPOCHS = 250
BATCH_SIZE = 32

LEARNING_RATE = 0.01
MOMENTUM = 0.9
WEIGHT_DECAY = 0.0

ETA_MIN = 1e-5

FOCAL_GAMMA = 2.0
LAMBDA_COARSE = 3.0
LAMBDA_CONSISTENCY = 1.0
CONSISTENCY_EPS = 1e-8
```

## result

E:\Anaconda3\envs\pytorch\python.exe C:\Users\18053\Desktop\流体识别\代码\LCRE-CFNet\eval.py 

Fine 六分类评估（最终任务）
Accuracy: 0.5652
Macro-F1: 0.3155
Weighted-F1: 0.5339
Balanced Accuracy: 0.3292

各类别 Precision / Recall / F1:
  气层: precision=0.5000, recall=0.3898, f1=0.4381
  含气层: precision=0.7536, recall=0.7761, f1=0.7647
  含气水层: precision=0.0000, recall=0.0000, f1=0.0000
  气水同层: precision=0.0000, recall=0.0000, f1=0.0000
  水层: precision=0.0000, recall=0.0000, f1=0.0000
  干层: precision=0.6018, recall=0.8095, f1=0.6904

混淆矩阵:
      气层  含气层  含气水层  气水同层  水层  干层
气层    23    8     3     0   0  25
含气层   13   52     0     0   0   2
含气水层   0    9     0     0   0   0
气水同层   6    0    10     0   0   1
水层     0    0     0     0   0  17
干层     4    0     8     0   4  68

预测标签分布:
class_name  true_count  true_ratio  pred_count  pred_ratio
        气层          59    0.233202          46    0.181818
       含气层          67    0.264822          69    0.272727
      含气水层           9    0.035573          21    0.083004
      气水同层          17    0.067194           0    0.000000
        水层          17    0.067194           4    0.015810
        干层          84    0.332016         113    0.446640

重点类别是否被识别: 否，Recall={'含气水层': 0.0, '气水同层': 0.0, '水层': 0.0}

Coarse 三分类评估（辅助分析）
Accuracy: 0.6957
Macro-F1: 0.6298
Weighted-F1: 0.7015
Balanced Accuracy: 0.6344

各类别 Precision / Recall / F1:
  气层优势类: precision=0.8661, recall=0.7698, f1=0.8151
  气水风险类: precision=0.3617, recall=0.3953, f1=0.3778
  非气层类: precision=0.6596, recall=0.7381, f1=0.6966

混淆矩阵:
       气层优势类  气水风险类  非气层类
气层优势类     97     14    15
气水风险类      9     17    17
非气层类       6     16    62

预测标签分布:
class_name  true_count  true_ratio  pred_count  pred_ratio
     气层优势类         126    0.498024         112    0.442688
     气水风险类          43    0.169960          47    0.185771
      非气层类          84    0.332016          94    0.371542

重点类别是否被识别: 是，气水风险类，Recall={'气水风险类': 0.3953488372093023}

Fine-as-Coarse 三分类评估（由 Fine 六分类概率聚合得到）
Accuracy: 0.6640
Macro-F1: 0.5846
Weighted-F1: 0.6561
Balanced Accuracy: 0.5823

各类别 Precision / Recall / F1:
  气层优势类: precision=0.7652, recall=0.8016, f1=0.7829
  气水风险类: precision=0.4062, recall=0.3023, f1=0.3467
  非气层类: precision=0.6067, recall=0.6429, f1=0.6243

混淆矩阵:
       气层优势类  气水风险类  非气层类
气层优势类    101      7    18
气水风险类     13     13    17
非气层类      18     12    54

预测标签分布:
class_name  true_count  true_ratio  pred_count  pred_ratio
     气层优势类         126    0.498024         132    0.521739
     气水风险类          43    0.169960          32    0.126482
      非气层类          84    0.332016          89    0.351779

重点类别是否被识别: 是，气水风险类，Recall={'气水风险类': 0.3023255813953488}

## model

```python
"""LCRE-CFNet coarse-fine model definition."""

from __future__ import annotations

import torch
from torch import nn


class LCRECFNet(nn.Module):
    """Shared DNN backbone with fine and coarse classification heads."""

    def __init__(
        self,
        input_dim: int,
        num_fine_classes: int,
        num_coarse_classes: int,
        dropout: float = 0.5,
    ) -> None:
        super().__init__()
        self.backbone = nn.Sequential(
            nn.Linear(input_dim, 100),
            nn.ReLU(),
            nn.Dropout(dropout),
            nn.Linear(100, 100),
            nn.ReLU(),
            nn.Dropout(dropout),
            nn.Linear(100, 50),
            nn.ReLU(),
            nn.Dropout(dropout),
        )
        self.fine_head = nn.Linear(50, num_fine_classes)
        self.coarse_head = nn.Linear(50, num_coarse_classes)

    def forward(self, x: torch.Tensor) -> tuple[torch.Tensor, torch.Tensor]:
        features = self.backbone(x)
        fine_logits = self.fine_head(features)
        coarse_logits = self.coarse_head(features)
        return fine_logits, coarse_logits
```

## loss

本模型采用多任务联合优化的策略，总损失函数由细分类损失、粗分类损失及一致性约束损失三部分加权构成，具体公式如下：


$$
\text{Total Loss} = \underbrace{\mathcal{L}_{\text{Fine}}}_{\text{细节准确度}} + \underbrace{\lambda_{\text{coarse}} \times \mathcal{L}_{\text{Coarse}}}_{\text{大局观}} + \underbrace{\lambda_{\text{consistency}} \times \mathcal{L}_{\text{Consistency}}}_{\text{逻辑一致性}}
$$


其中各项的具体定义与物理意义如下：

- **$\mathcal{L}_{\text{Fine}}$（细分类损失）**：等于 **交叉熵损失**。
    - 它负责监督模型对 6 种具体岩性/流体（气、水、干层等）的预测能力，是模型的基础判别指标。
- **$\mathcal{L}_{\text{Coarse}}$（粗分类损失）**：等于 **焦点损失**。
    - 它负责监督模型对 3 大类（气层优势、气水风险、非气层）的预测。引入 `Focal Loss` 旨在解决粗分类中样本不均衡的问题，让模型更关注难以识别的“气水风险”类别。
- **$\mathcal{L}_{\text{Consistency}}$（一致性损失）**：等于 **粗细粒度一致性约束**。
    - 它强制要求“细分类的预测结果”在逻辑上必须归属于“粗分类的预测范畴”。例如，如果细分类预测为“气层”，粗分类就不应预测为“水层”。这一项通过惩罚逻辑冲突来规范模型的输出空间。
- **$\lambda_{\text{coarse}}$ 与 $\lambda_{\text{consistency}}$**：等于 **超参数权重**。
    - 分别控制粗分类任务和一致性约束在总损失中的贡献比例，用于平衡不同任务的学习速率。



# 2

## config

```
"""LCRE-CFNet coarse-fine baseline configuration."""

from __future__ import annotations

from pathlib import Path


# =========================
# Paths
# =========================

SCRIPT_DIR = Path(__file__).resolve().parent

DEFAULT_TRAIN_CSV = "../../dataset/all_features/train_val/train.csv"
DEFAULT_TEST_CSV = "../../dataset/all_features/train_val/test.csv"


# =========================
# Experiment
# =========================

EXPERIMENT_NAME = "LCRE-CFNet_13_CoarseFine_Consistency_LAMBDA3.0_C1.0"
RANDOM_SEED = 42


# =========================
# Data
# =========================

# FEATURES = [
#     "GR",
#     "SP",
#     "AC",
#     "CAL",
#     "DEN1",
#     "CNL1",
#     "PE",
#     "log（RD）",
#     "SW",
#     "RD_RS_ratio",
# ]

FEATURES = [
    "GR",
    "SP",
    "AC",
    "AC1",
    "CAL",
    "U",
    "DEN",
    "DEN1",
    "CNL",
    "CNL1",
    "PE",
    "RD",
    "RS",
    "log（RD）",
    "log（RS）",
    "? Φ1",
    "? Φ2",
    "? Φ3",
# # 新增黄金物理特征
# #     "RWA",
# #     "VSH_GR",
# #     "PHI_DENSITY",
# #     "EXCAVATION_DIFF",
# #     "INVASION_DIFF",
]

LABEL_CANDIDATES = ["试油结论", "y_fine"]

MISSING_VALUES = [
    -9999,
    "-9999",
    "#REF!",
    "",
    " ",
    "NA",
    "N/A",
    "nan",
    "NaN",
    "None",
]

CLASS_NAMES = ["气层", "含气层", "含气水层", "气水同层", "水层", "干层"]
NUM_CLASSES = len(CLASS_NAMES)

COARSE_CLASS_NAMES = ["气层优势类", "气水风险类", "非气层类"]
NUM_COARSE_CLASSES = len(COARSE_CLASS_NAMES)

COARSE_MAPPING = {
    "气层": "气层优势类",
    "含气层": "气层优势类",
    "含气水层": "气水风险类",
    "气水同层": "气水风险类",
    "水层": "气水风险类",
    "干层": "非气层类",
}


# =========================
# Model
# =========================

HIDDEN_DIMS = [100, 100, 50]
DROPOUT = 0.5


# =========================
# Training
# =========================

EPOCHS = 250
BATCH_SIZE = 32

LEARNING_RATE = 0.01
MOMENTUM = 0.9
WEIGHT_DECAY = 0.0

ETA_MIN = 1e-5

FOCAL_GAMMA = 2.0
LAMBDA_COARSE = 3.0
LAMBDA_CONSISTENCY = 1.0
CONSISTENCY_EPS = 1e-8
```

## result

E:\Anaconda3\envs\pytorch\python.exe C:\Users\18053\Desktop\流体识别\代码\LCRE-CFNet\eval.py 

Fine 六分类评估（最终任务）
Accuracy: 0.5059
Macro-F1: 0.2698
Weighted-F1: 0.4586
Balanced Accuracy: 0.2971

各类别 Precision / Recall / F1:
  气层: precision=0.4247, recall=0.5254, f1=0.4697
  含气层: precision=0.4595, recall=0.5075, f1=0.4823
  含气水层: precision=0.0000, recall=0.0000, f1=0.0000
  气水同层: precision=0.0000, recall=0.0000, f1=0.0000
  水层: precision=0.0000, recall=0.0000, f1=0.0000
  干层: precision=0.6000, recall=0.7500, f1=0.6667

混淆矩阵:
      气层  含气层  含气水层  气水同层  水层  干层
气层    31   27     0     0   0   1
含气层   18   34     0     0   0  15
含气水层   1    8     0     0   0   0
气水同层   7    0     1     0   0   9
水层     0    0     0     0   0  17
干层    16    5     0     0   0  63

预测标签分布:
class_name  true_count  true_ratio  pred_count  pred_ratio
        气层          59    0.233202          73    0.288538
       含气层          67    0.264822          74    0.292490
      含气水层           9    0.035573           1    0.003953
      气水同层          17    0.067194           0    0.000000
        水层          17    0.067194           0    0.000000
        干层          84    0.332016         105    0.415020

重点类别是否被识别: 否，Recall={'含气水层': 0.0, '气水同层': 0.0, '水层': 0.0}

Coarse 三分类评估（辅助分析）
Accuracy: 0.5810
Macro-F1: 0.4782
Weighted-F1: 0.5806
Balanced Accuracy: 0.4821

各类别 Precision / Recall / F1:
  气层优势类: precision=0.7458, recall=0.6984, f1=0.7213
  气水风险类: precision=0.0976, recall=0.0930, f1=0.0952
  非气层类: precision=0.5851, recall=0.6548, f1=0.6180

混淆矩阵:
       气层优势类  气水风险类  非气层类
气层优势类     88     24    14
气水风险类     14      4    25
非气层类      16     13    55

预测标签分布:
class_name  true_count  true_ratio  pred_count  pred_ratio
     气层优势类         126    0.498024         118    0.466403
     气水风险类          43    0.169960          41    0.162055
      非气层类          84    0.332016          94    0.371542

重点类别是否被识别: 是，气水风险类，Recall={'气水风险类': 0.09302325581395349}

Fine-as-Coarse 三分类评估（由 Fine 六分类概率聚合得到）
Accuracy: 0.6719
Macro-F1: 0.5053
Weighted-F1: 0.6236
Balanced Accuracy: 0.5353

各类别 Precision / Recall / F1:
  气层优势类: precision=0.7400, recall=0.8810, f1=0.8043
  气水风险类: precision=0.3333, recall=0.0465, f1=0.0816
  非气层类: precision=0.5876, recall=0.6786, f1=0.6298

混淆矩阵:
       气层优势类  气水风险类  非气层类
气层优势类    111      1    14
气水风险类     15      2    26
非气层类      24      3    57

预测标签分布:
class_name  true_count  true_ratio  pred_count  pred_ratio
     气层优势类         126    0.498024         150    0.592885
     气水风险类          43    0.169960           6    0.023715
      非气层类          84    0.332016          97    0.383399

重点类别是否被识别: 是，气水风险类，Recall={'气水风险类': 0.046511627906976744}

最终说明：fine 六分类结果为主；coarse 三分类和 fine-as-coarse 三分类仅作辅助分析；未用 coarse_pred 硬约束 fine_pred。

## model

```python
"""LCRE-CFNet coarse-fine model definition."""

from __future__ import annotations

import torch
from torch import nn


class LCRECFNet(nn.Module):
    """Shared DNN backbone with fine and coarse classification heads."""

    def __init__(
        self,
        input_dim: int,
        num_fine_classes: int,
        num_coarse_classes: int,
        dropout: float = 0.5,
    ) -> None:
        super().__init__()
        self.backbone = nn.Sequential(
            nn.Linear(input_dim, 100),
            nn.ReLU(),
            nn.Dropout(dropout),
            nn.Linear(100, 100),
            nn.ReLU(),
            nn.Dropout(dropout),
            nn.Linear(100, 50),
            nn.ReLU(),
            nn.Dropout(dropout),
        )
        self.fine_head = nn.Linear(50, num_fine_classes)
        self.coarse_head = nn.Linear(50, num_coarse_classes)

    def forward(self, x: torch.Tensor) -> tuple[torch.Tensor, torch.Tensor]:
        features = self.backbone(x)
        fine_logits = self.fine_head(features)
        coarse_logits = self.coarse_head(features)
        return fine_logits, coarse_logits
```

## loss

本模型采用多任务联合优化的策略，总损失函数由细分类损失、粗分类损失及一致性约束损失三部分加权构成，具体公式如下：


$$
\text{Total Loss} = \underbrace{\mathcal{L}_{\text{Fine}}}_{\text{细节准确度}} + \underbrace{\lambda_{\text{coarse}} \times \mathcal{L}_{\text{Coarse}}}_{\text{大局观}} + \underbrace{\lambda_{\text{consistency}} \times \mathcal{L}_{\text{Consistency}}}_{\text{逻辑一致性}}
$$


其中各项的具体定义与物理意义如下：

- **$\mathcal{L}_{\text{Fine}}$（细分类损失）**：等于 **交叉熵损失**。
  - 它负责监督模型对 6 种具体岩性/流体（气、水、干层等）的预测能力，是模型的基础判别指标。
- **$\mathcal{L}_{\text{Coarse}}$（粗分类损失）**：等于 **焦点损失**。
  - 它负责监督模型对 3 大类（气层优势、气水风险、非气层）的预测。引入 `Focal Loss` 旨在解决粗分类中样本不均衡的问题，让模型更关注难以识别的“气水风险”类别。
- **$\mathcal{L}_{\text{Consistency}}$（一致性损失）**：等于 **粗细粒度一致性约束**。
  - 它强制要求“细分类的预测结果”在逻辑上必须归属于“粗分类的预测范畴”。例如，如果细分类预测为“气层”，粗分类就不应预测为“水层”。这一项通过惩罚逻辑冲突来规范模型的输出空间。
- **$\lambda_{\text{coarse}}$ 与 $\lambda_{\text{consistency}}$**：等于 **超参数权重**。
  - 分别控制粗分类任务和一致性约束在总损失中的贡献比例，用于平衡不同任务的学习速率。



# 3

## config

```
"""LCRE-CFNet coarse-fine baseline configuration."""

from __future__ import annotations

from pathlib import Path


# =========================
# Paths
# =========================

SCRIPT_DIR = Path(__file__).resolve().parent

DEFAULT_TRAIN_CSV = "../../dataset/all_features/train_val/train.csv"
DEFAULT_TEST_CSV = "../../dataset/all_features/train_val/test.csv"


# =========================
# Experiment
# =========================

EXPERIMENT_NAME = "LCRE-CFNet_13_CoarseFine_Consistency_LAMBDA3.0_C1.0"
RANDOM_SEED = 42


# =========================
# Data
# =========================

FEATURES = [
    "GR",
    "SP",
    "AC1",
    "CAL",
    "DEN1",
    "CNL1",
    "PE",
    "log（RD）",
    # "SW",
    "RD_RS_ratio",
]

# FEATURES = [
#     "GR",
#     "SP",
#     "AC",
#     "AC1",
#     "CAL",
#     "U",
#     "DEN",
#     "DEN1",
#     "CNL",
#     "CNL1",
#     "PE",
#     "RD",
#     "RS",
#     "log（RD）",
#     "log（RS）",
#     "? Φ1",
#     "? Φ2",
#     "? Φ3",
# # 新增黄金物理特征
# #     "RWA",
# #     "VSH_GR",
# #     "PHI_DENSITY",
# #     "EXCAVATION_DIFF",
# #     "INVASION_DIFF",
# ]

LABEL_CANDIDATES = ["试油结论", "y_fine"]

MISSING_VALUES = [
    -9999,
    "-9999",
    "#REF!",
    "",
    " ",
    "NA",
    "N/A",
    "nan",
    "NaN",
    "None",
]

CLASS_NAMES = ["气层", "含气层", "含气水层", "气水同层", "水层", "干层"]
NUM_CLASSES = len(CLASS_NAMES)

COARSE_CLASS_NAMES = ["气层优势类", "气水风险类", "非气层类"]
NUM_COARSE_CLASSES = len(COARSE_CLASS_NAMES)

COARSE_MAPPING = {
    "气层": "气层优势类",
    "含气层": "气层优势类",
    "含气水层": "气水风险类",
    "气水同层": "气水风险类",
    "水层": "气水风险类",
    "干层": "非气层类",
}


# =========================
# Model
# =========================

HIDDEN_DIMS = [100, 100, 50]
DROPOUT = 0.5


# =========================
# Training
# =========================

EPOCHS = 250
BATCH_SIZE = 32

LEARNING_RATE = 0.01
MOMENTUM = 0.9
WEIGHT_DECAY = 0.0

ETA_MIN = 1e-5

FOCAL_GAMMA = 2.0
LAMBDA_COARSE = 3.0
LAMBDA_CONSISTENCY = 1.0
CONSISTENCY_EPS = 1e-8
```

## result

E:\Anaconda3\envs\pytorch\python.exe C:\Users\18053\Desktop\流体识别\代码\LCRE-CFNet\eval.py 

Fine 六分类评估（最终任务）
Accuracy: 0.3478
Macro-F1: 0.1983
Weighted-F1: 0.3367
Balanced Accuracy: 0.2040

各类别 Precision / Recall / F1:
  气层: precision=0.4483, recall=0.4407, f1=0.4444
  含气层: precision=0.2055, recall=0.2239, f1=0.2143
  含气水层: precision=0.0000, recall=0.0000, f1=0.0000
  气水同层: precision=0.0000, recall=0.0000, f1=0.0000
  水层: precision=0.0000, recall=0.0000, f1=0.0000
  干层: precision=0.5054, recall=0.5595, f1=0.5311

混淆矩阵:
      气层  含气层  含气水层  气水同层  水层  干层
气层    26   21     0     0   8   4
含气层   30   15     2     0   0  20
含气水层   0    9     0     0   0   0
气水同层   0    5     0     0   7   5
水层     0    0     0     0   0  17
干层     2   23     4     0   8  47

预测标签分布:
class_name  true_count  true_ratio  pred_count  pred_ratio
        气层          59    0.233202          58    0.229249
       含气层          67    0.264822          73    0.288538
      含气水层           9    0.035573           6    0.023715
      气水同层          17    0.067194           0    0.000000
        水层          17    0.067194          23    0.090909
        干层          84    0.332016          93    0.367589

重点类别是否被识别: 否，Recall={'含气水层': 0.0, '气水同层': 0.0, '水层': 0.0}

Coarse 三分类评估（辅助分析）
Accuracy: 0.4822
Macro-F1: 0.4124
Weighted-F1: 0.4941
Balanced Accuracy: 0.4073

各类别 Precision / Recall / F1:
  气层优势类: precision=0.6585, recall=0.6429, f1=0.6506
  气水风险类: precision=0.1290, recall=0.1860, f1=0.1524
  非气层类: precision=0.4853, recall=0.3929, f1=0.4342

混淆矩阵:
       气层优势类  气水风险类  非气层类
气层优势类     81     31    14
气水风险类     14      8    21
非气层类      28     23    33

预测标签分布:
class_name  true_count  true_ratio  pred_count  pred_ratio
     气层优势类         126    0.498024         123    0.486166
     气水风险类          43    0.169960          62    0.245059
      非气层类          84    0.332016          68    0.268775

重点类别是否被识别: 是，气水风险类，Recall={'气水风险类': 0.18604651162790697}

Fine-as-Coarse 三分类评估（由 Fine 六分类概率聚合得到）
Accuracy: 0.4862
Macro-F1: 0.3847
Weighted-F1: 0.4639
Balanced Accuracy: 0.3916

各类别 Precision / Recall / F1:
  气层优势类: precision=0.5962, recall=0.7381, f1=0.6596
  气水风险类: precision=0.1944, recall=0.1628, f1=0.1772
  非气层类: precision=0.3770, recall=0.2738, f1=0.3172

混淆矩阵:
       气层优势类  气水风险类  非气层类
气层优势类     93     17    16
气水风险类     14      7    22
非气层类      49     12    23

预测标签分布:
class_name  true_count  true_ratio  pred_count  pred_ratio
     气层优势类         126    0.498024         156    0.616601
     气水风险类          43    0.169960          36    0.142292
      非气层类          84    0.332016          61    0.241107

重点类别是否被识别: 是，气水风险类，Recall={'气水风险类': 0.16279069767441862}

最终说明：fine 六分类结果为主；coarse 三分类和 fine-as-coarse 三分类仅作辅助分析；未用 coarse_pred 硬约束 fine_pred。
评估结果已保存至: C:\Users\18053\Desktop\流体识别\代码\LCRE-CFNet\outputs\LCRE-CFNet_13_CoarseFine_Consistency_LAMBDA3.0_C1.0\eval

Process finished with exit code 0



## model

```python
"""LCRE-CFNet coarse-fine model definition."""

from __future__ import annotations

import torch
from torch import nn


class LCRECFNet(nn.Module):
    """Shared DNN backbone with fine and coarse classification heads."""

    def __init__(
        self,
        input_dim: int,
        num_fine_classes: int,
        num_coarse_classes: int,
        dropout: float = 0.5,
    ) -> None:
        super().__init__()
        self.backbone = nn.Sequential(
            nn.Linear(input_dim, 100),
            nn.ReLU(),
            nn.Dropout(dropout),
            nn.Linear(100, 100),
            nn.ReLU(),
            nn.Dropout(dropout),
            nn.Linear(100, 50),
            nn.ReLU(),
            nn.Dropout(dropout),
        )
        self.fine_head = nn.Linear(50, num_fine_classes)
        self.coarse_head = nn.Linear(50, num_coarse_classes)

    def forward(self, x: torch.Tensor) -> tuple[torch.Tensor, torch.Tensor]:
        features = self.backbone(x)
        fine_logits = self.fine_head(features)
        coarse_logits = self.coarse_head(features)
        return fine_logits, coarse_logits
```

## loss

本模型采用多任务联合优化的策略，总损失函数由细分类损失、粗分类损失及一致性约束损失三部分加权构成，具体公式如下：


$$
\text{Total Loss} = \underbrace{\mathcal{L}_{\text{Fine}}}_{\text{细节准确度}} + \underbrace{\lambda_{\text{coarse}} \times \mathcal{L}_{\text{Coarse}}}_{\text{大局观}} + \underbrace{\lambda_{\text{consistency}} \times \mathcal{L}_{\text{Consistency}}}_{\text{逻辑一致性}}
$$


其中各项的具体定义与物理意义如下：

- **$\mathcal{L}_{\text{Fine}}$（细分类损失）**：等于 **交叉熵损失**。
  - 它负责监督模型对 6 种具体岩性/流体（气、水、干层等）的预测能力，是模型的基础判别指标。
- **$\mathcal{L}_{\text{Coarse}}$（粗分类损失）**：等于 **焦点损失**。
  - 它负责监督模型对 3 大类（气层优势、气水风险、非气层）的预测。引入 `Focal Loss` 旨在解决粗分类中样本不均衡的问题，让模型更关注难以识别的“气水风险”类别。
- **$\mathcal{L}_{\text{Consistency}}$（一致性损失）**：等于 **粗细粒度一致性约束**。
  - 它强制要求“细分类的预测结果”在逻辑上必须归属于“粗分类的预测范畴”。例如，如果细分类预测为“气层”，粗分类就不应预测为“水层”。这一项通过惩罚逻辑冲突来规范模型的输出空间。
- **$\lambda_{\text{coarse}}$ 与 $\lambda_{\text{consistency}}$**：等于 **超参数权重**。
  - 分别控制粗分类任务和一致性约束在总损失中的贡献比例，用于平衡不同任务的学习速率。

# 4

## config

```
"""LCRE-CFNet coarse-fine baseline configuration."""

from __future__ import annotations

from pathlib import Path


# =========================
# Paths
# =========================

SCRIPT_DIR = Path(__file__).resolve().parent

DEFAULT_TRAIN_CSV = "../../dataset/train_val/train.csv"
DEFAULT_TEST_CSV = "../../dataset/train_val/test.csv"


# =========================
# Experiment
# =========================

EXPERIMENT_NAME = "LCRE-CFNet_Raw9NoSW_Window0p5m_CoarseFine_Consistency_LAMBDA3.0_C1.0"
RANDOM_SEED = 42


# =========================
# Data
# =========================
FEATURES = [
    "GR",
    "SP",
    "AC",
    "CAL",
    "DEN",
    "CNL",
    "PE",
    "RD",
    "RD_RS_ratio",

    "GR_win0p5m_mean",
    "GR_win0p5m_std",
    "GR_win0p5m_range",
    "GR_win0p5m_slope",

    "SP_win0p5m_mean",
    "SP_win0p5m_std",
    "SP_win0p5m_range",
    "SP_win0p5m_slope",

    "AC_win0p5m_mean",
    "AC_win0p5m_std",
    "AC_win0p5m_range",
    "AC_win0p5m_slope",

    "CAL_win0p5m_mean",
    "CAL_win0p5m_std",
    "CAL_win0p5m_range",
    "CAL_win0p5m_slope",

    "DEN_win0p5m_mean",
    "DEN_win0p5m_std",
    "DEN_win0p5m_range",
    "DEN_win0p5m_slope",

    "CNL_win0p5m_mean",
    "CNL_win0p5m_std",
    "CNL_win0p5m_range",
    "CNL_win0p5m_slope",

    "PE_win0p5m_mean",
    "PE_win0p5m_std",
    "PE_win0p5m_range",
    "PE_win0p5m_slope",

    "RD_win0p5m_mean",
    "RD_win0p5m_std",
    "RD_win0p5m_range",
    "RD_win0p5m_slope",

    "RD_RS_ratio_win0p5m_mean",
    "RD_RS_ratio_win0p5m_std",
    "RD_RS_ratio_win0p5m_range",
    "RD_RS_ratio_win0p5m_slope",

    # "SW",
    # "SW_win0p5m_mean",
    # "SW_win0p5m_std",
    # "SW_win0p5m_range",
    # "SW_win0p5m_slope",

    "win0p5m_count",
]


# FEATURES = [
#     "GR",
#     "SP",
#     "AC",
#     "CAL",
#     "DEN",
#     "CNL",
#     "PE",
#     "log（RD）",
#     # "SW",
#     "RD_RS_ratio",
# ]

# FEATURES = [
#     "GR",
#     "SP",
#     "AC",
#     "AC1",
#     "CAL",
#     "U",
#     "DEN",
#     "DEN1",
#     "CNL",
#     "CNL1",
#     "PE",
#     "RD",
#     "RS",
#     "log（RD）",
#     "log（RS）",
#     "? Φ1",
#     "? Φ2",
#     "? Φ3",
# # 新增黄金物理特征
# #     "RWA",
# #     "VSH_GR",
# #     "PHI_DENSITY",
# #     "EXCAVATION_DIFF",
# #     "INVASION_DIFF",
# ]

LABEL_CANDIDATES = ["试油结论", "y_fine"]

MISSING_VALUES = [
    -9999,
    "-9999",
    "#REF!",
    "",
    " ",
    "NA",
    "N/A",
    "nan",
    "NaN",
    "None",
]

CLASS_NAMES = ["气层", "含气层", "含气水层", "气水同层", "水层", "干层"]
NUM_CLASSES = len(CLASS_NAMES)

COARSE_CLASS_NAMES = ["气层优势类", "气水风险类", "非气层类"]
NUM_COARSE_CLASSES = len(COARSE_CLASS_NAMES)

COARSE_MAPPING = {
    "气层": "气层优势类",
    "含气层": "气层优势类",
    "含气水层": "气水风险类",
    "气水同层": "气水风险类",
    "水层": "气水风险类",
    "干层": "非气层类",
}


# =========================
# Model
# =========================

HIDDEN_DIMS = [100, 100, 50]
DROPOUT = 0.5


# =========================
# Training
# =========================

EPOCHS = 250
BATCH_SIZE = 32

LEARNING_RATE = 0.01
MOMENTUM = 0.9
WEIGHT_DECAY = 0.0

ETA_MIN = 1e-5

FOCAL_GAMMA = 2.0
LAMBDA_COARSE = 3.0
LAMBDA_CONSISTENCY = 1.0
CONSISTENCY_EPS = 1e-8
```

## result

E:\Anaconda3\envs\pytorch\python.exe C:\Users\18053\Desktop\流体识别\代码\LCRE-CFNet\eval.py 

Fine 六分类评估（最终任务）
Accuracy: 0.3755
Macro-F1: 0.2068
Weighted-F1: 0.3500
Balanced Accuracy: 0.2213

各类别 Precision / Recall / F1:
  气层: precision=0.3810, recall=0.4068, f1=0.3934
  含气层: precision=0.3205, recall=0.3731, f1=0.3448
  含气水层: precision=0.0000, recall=0.0000, f1=0.0000
  气水同层: precision=0.0000, recall=0.0000, f1=0.0000
  水层: precision=0.0000, recall=0.0000, f1=0.0000
  干层: precision=0.4646, recall=0.5476, f1=0.5027

混淆矩阵:
      气层  含气层  含气水层  气水同层  水层  干层
气层    24   15     5     0   0  15
含气层    0   25     8     0   0  34
含气水层   0    9     0     0   0   0
气水同层   1   15     0     0   0   1
水层     2   12     0     0   0   3
干层    36    2     0     0   0  46

预测标签分布:
class_name  true_count  true_ratio  pred_count  pred_ratio
        气层          59    0.233202          63    0.249012
       含气层          67    0.264822          78    0.308300
      含气水层           9    0.035573          13    0.051383
      气水同层          17    0.067194           0    0.000000
        水层          17    0.067194           0    0.000000
        干层          84    0.332016          99    0.391304

重点类别是否被识别: 否，Recall={'含气水层': 0.0, '气水同层': 0.0, '水层': 0.0}

Coarse 三分类评估（辅助分析）
Accuracy: 0.3794
Macro-F1: 0.3276
Weighted-F1: 0.3964
Balanced Accuracy: 0.3148

各类别 Precision / Recall / F1:
  气层优势类: precision=0.4505, recall=0.3968, f1=0.4219
  气水风险类: precision=0.0000, recall=0.0000, f1=0.0000
  非气层类: precision=0.5750, recall=0.5476, f1=0.5610

混淆矩阵:
       气层优势类  气水风险类  非气层类
气层优势类     50     43    33
气水风险类     42      0     1
非气层类      19     19    46

预测标签分布:
class_name  true_count  true_ratio  pred_count  pred_ratio
     气层优势类         126    0.498024         111    0.438735
     气水风险类          43    0.169960          62    0.245059
      非气层类          84    0.332016          80    0.316206

重点类别是否被识别: 否，Recall={'气水风险类': 0.0}

Fine-as-Coarse 三分类评估（由 Fine 六分类概率聚合得到）
Accuracy: 0.4269
Macro-F1: 0.3425
Weighted-F1: 0.4189
Balanced Accuracy: 0.3452

各类别 Precision / Recall / F1:
  气层优势类: precision=0.4406, recall=0.5000, f1=0.4684
  气水风险类: precision=0.0000, recall=0.0000, f1=0.0000
  非气层类: precision=0.5844, recall=0.5357, f1=0.5590

混淆矩阵:
       气层优势类  气水风险类  非气层类
气层优势类     63     31    32
气水风险类     43      0     0
非气层类      37      2    45

预测标签分布:
class_name  true_count  true_ratio  pred_count  pred_ratio
     气层优势类         126    0.498024         143    0.565217
     气水风险类          43    0.169960          33    0.130435
      非气层类          84    0.332016          77    0.304348

重点类别是否被识别: 否，Recall={'气水风险类': 0.0}

最终说明：fine 六分类结果为主；coarse 三分类和 fine-as-coarse 三分类仅作辅助分析；未用 coarse_pred 硬约束 fine_pred。
评估结果已保存至: C:\Users\18053\Desktop\流体识别\代码\LCRE-CFNet\outputs\LCRE-CFNet_Raw9NoSW_Window0p5m_CoarseFine_Consistency_LAMBDA3.0_C1.0\eval

Process finished with exit code 0

## model

```python
"""LCRE-CFNet coarse-fine model definition."""

from __future__ import annotations

import torch
from torch import nn


class LCRECFNet(nn.Module):
    """Shared DNN backbone with fine and coarse classification heads."""

    def __init__(
        self,
        input_dim: int,
        num_fine_classes: int,
        num_coarse_classes: int,
        dropout: float = 0.5,
    ) -> None:
        super().__init__()
        self.backbone = nn.Sequential(
            nn.Linear(input_dim, 100),
            nn.ReLU(),
            nn.Dropout(dropout),
            nn.Linear(100, 100),
            nn.ReLU(),
            nn.Dropout(dropout),
            nn.Linear(100, 50),
            nn.ReLU(),
            nn.Dropout(dropout),
        )
        self.fine_head = nn.Linear(50, num_fine_classes)
        self.coarse_head = nn.Linear(50, num_coarse_classes)

    def forward(self, x: torch.Tensor) -> tuple[torch.Tensor, torch.Tensor]:
        features = self.backbone(x)
        fine_logits = self.fine_head(features)
        coarse_logits = self.coarse_head(features)
        return fine_logits, coarse_logits
```

## loss

本模型采用多任务联合优化的策略，总损失函数由细分类损失、粗分类损失及一致性约束损失三部分加权构成，具体公式如下：


$$
\text{Total Loss} = \underbrace{\mathcal{L}_{\text{Fine}}}_{\text{细节准确度}} + \underbrace{\lambda_{\text{coarse}} \times \mathcal{L}_{\text{Coarse}}}_{\text{大局观}} + \underbrace{\lambda_{\text{consistency}} \times \mathcal{L}_{\text{Consistency}}}_{\text{逻辑一致性}}
$$


其中各项的具体定义与物理意义如下：

- **$\mathcal{L}_{\text{Fine}}$（细分类损失）**：等于 **交叉熵损失**。
  - 它负责监督模型对 6 种具体岩性/流体（气、水、干层等）的预测能力，是模型的基础判别指标。
- **$\mathcal{L}_{\text{Coarse}}$（粗分类损失）**：等于 **焦点损失**。
  - 它负责监督模型对 3 大类（气层优势、气水风险、非气层）的预测。引入 `Focal Loss` 旨在解决粗分类中样本不均衡的问题，让模型更关注难以识别的“气水风险”类别。
- **$\mathcal{L}_{\text{Consistency}}$（一致性损失）**：等于 **粗细粒度一致性约束**。
  - 它强制要求“细分类的预测结果”在逻辑上必须归属于“粗分类的预测范畴”。例如，如果细分类预测为“气层”，粗分类就不应预测为“水层”。这一项通过惩罚逻辑冲突来规范模型的输出空间。
- **$\lambda_{\text{coarse}}$ 与 $\lambda_{\text{consistency}}$**：等于 **超参数权重**。
  - 分别控制粗分类任务和一致性约束在总损失中的贡献比例，用于平衡不同任务的学习速率。

# 5

## config

```
"""LCRE-CFNet coarse-fine baseline configuration."""

from __future__ import annotations

from pathlib import Path


# =========================
# Paths
# =========================

SCRIPT_DIR = Path(__file__).resolve().parent

DEFAULT_TRAIN_CSV = "../../dataset/train_val/train.csv"
DEFAULT_TEST_CSV = "../../dataset/train_val/test.csv"


# =========================
# Experiment
# =========================

EXPERIMENT_NAME = "LCRE-CFNet_Raw9WithSW_Window0p5m_CoarseFine_Consistency_LAMBDA3.0_C1.0"
RANDOM_SEED = 42


# =========================
# Data
# =========================
FEATURES = [
    "GR",
    "SP",
    "AC",
    "CAL",
    "DEN",
    "CNL",
    "PE",
    "RD",
    "RD_RS_ratio",

    "GR_win0p5m_mean",
    "GR_win0p5m_std",
    "GR_win0p5m_range",
    "GR_win0p5m_slope",

    "SP_win0p5m_mean",
    "SP_win0p5m_std",
    "SP_win0p5m_range",
    "SP_win0p5m_slope",

    "AC_win0p5m_mean",
    "AC_win0p5m_std",
    "AC_win0p5m_range",
    "AC_win0p5m_slope",

    "CAL_win0p5m_mean",
    "CAL_win0p5m_std",
    "CAL_win0p5m_range",
    "CAL_win0p5m_slope",

    "DEN_win0p5m_mean",
    "DEN_win0p5m_std",
    "DEN_win0p5m_range",
    "DEN_win0p5m_slope",

    "CNL_win0p5m_mean",
    "CNL_win0p5m_std",
    "CNL_win0p5m_range",
    "CNL_win0p5m_slope",

    "PE_win0p5m_mean",
    "PE_win0p5m_std",
    "PE_win0p5m_range",
    "PE_win0p5m_slope",

    "RD_win0p5m_mean",
    "RD_win0p5m_std",
    "RD_win0p5m_range",
    "RD_win0p5m_slope",

    "RD_RS_ratio_win0p5m_mean",
    "RD_RS_ratio_win0p5m_std",
    "RD_RS_ratio_win0p5m_range",
    "RD_RS_ratio_win0p5m_slope",

    "SW",
    "SW_win0p5m_mean",
    "SW_win0p5m_std",
    "SW_win0p5m_range",
    "SW_win0p5m_slope",

    "win0p5m_count",
]


# FEATURES = [
#     "GR",
#     "SP",
#     "AC",
#     "CAL",
#     "DEN",
#     "CNL",
#     "PE",
#     "log（RD）",
#     # "SW",
#     "RD_RS_ratio",
# ]

# FEATURES = [
#     "GR",
#     "SP",
#     "AC",
#     "AC1",
#     "CAL",
#     "U",
#     "DEN",
#     "DEN1",
#     "CNL",
#     "CNL1",
#     "PE",
#     "RD",
#     "RS",
#     "log（RD）",
#     "log（RS）",
#     "? Φ1",
#     "? Φ2",
#     "? Φ3",
# # 新增黄金物理特征
# #     "RWA",
# #     "VSH_GR",
# #     "PHI_DENSITY",
# #     "EXCAVATION_DIFF",
# #     "INVASION_DIFF",
# ]

LABEL_CANDIDATES = ["试油结论", "y_fine"]

MISSING_VALUES = [
    -9999,
    "-9999",
    "#REF!",
    "",
    " ",
    "NA",
    "N/A",
    "nan",
    "NaN",
    "None",
]

CLASS_NAMES = ["气层", "含气层", "含气水层", "气水同层", "水层", "干层"]
NUM_CLASSES = len(CLASS_NAMES)

COARSE_CLASS_NAMES = ["气层优势类", "气水风险类", "非气层类"]
NUM_COARSE_CLASSES = len(COARSE_CLASS_NAMES)

COARSE_MAPPING = {
    "气层": "气层优势类",
    "含气层": "气层优势类",
    "含气水层": "气水风险类",
    "气水同层": "气水风险类",
    "水层": "气水风险类",
    "干层": "非气层类",
}


# =========================
# Model
# =========================

HIDDEN_DIMS = [100, 100, 50]
DROPOUT = 0.5


# =========================
# Training
# =========================

EPOCHS = 250
BATCH_SIZE = 32

LEARNING_RATE = 0.01
MOMENTUM = 0.9
WEIGHT_DECAY = 0.0

ETA_MIN = 1e-5

FOCAL_GAMMA = 2.0
LAMBDA_COARSE = 3.0
LAMBDA_CONSISTENCY = 1.0
CONSISTENCY_EPS = 1e-8
```

## result

E:\Anaconda3\envs\pytorch\python.exe C:\Users\18053\Desktop\流体识别\代码\LCRE-CFNet\eval.py 

Fine 六分类评估（最终任务）
Accuracy: 0.3755
Macro-F1: 0.1819
Weighted-F1: 0.3197
Balanced Accuracy: 0.2120

各类别 Precision / Recall / F1:
  气层: precision=0.2651, recall=0.3729, f1=0.3099
  含气层: precision=0.2222, recall=0.1493, f1=0.1786
  含气水层: precision=0.0000, recall=0.0000, f1=0.0000
  气水同层: precision=0.0000, recall=0.0000, f1=0.0000
  水层: precision=0.0000, recall=0.0000, f1=0.0000
  干层: precision=0.5040, recall=0.7500, f1=0.6029

混淆矩阵:
      气层  含气层  含气水层  气水同层  水层  干层
气层    22   17     0     0   0  20
含气层   19   10     0     0   0  38
含气水层   0    9     0     0   0   0
气水同层  13    4     0     0   0   0
水层     8    5     0     0   0   4
干层    21    0     0     0   0  63

预测标签分布:
class_name  true_count  true_ratio  pred_count  pred_ratio
        气层          59    0.233202          83    0.328063
       含气层          67    0.264822          45    0.177866
      含气水层           9    0.035573           0    0.000000
      气水同层          17    0.067194           0    0.000000
        水层          17    0.067194           0    0.000000
        干层          84    0.332016         125    0.494071

重点类别是否被识别: 否，Recall={'含气水层': 0.0, '气水同层': 0.0, '水层': 0.0}

Coarse 三分类评估（辅助分析）
Accuracy: 0.3794
Macro-F1: 0.3569
Weighted-F1: 0.4204
Balanced Accuracy: 0.3279

各类别 Precision / Recall / F1:
  气层优势类: precision=0.5000, recall=0.3413, f1=0.4057
  气水风险类: precision=0.0110, recall=0.0233, f1=0.0149
  非气层类: precision=0.6842, recall=0.6190, f1=0.6500

混淆矩阵:
       气层优势类  气水风险类  非气层类
气层优势类     43     61    22
气水风险类     40      1     2
非气层类       3     29    52

预测标签分布:
class_name  true_count  true_ratio  pred_count  pred_ratio
     气层优势类         126    0.498024          86    0.339921
     气水风险类          43    0.169960          91    0.359684
      非气层类          84    0.332016          76    0.300395

重点类别是否被识别: 是，气水风险类，Recall={'气水风险类': 0.023255813953488372}

Fine-as-Coarse 三分类评估（由 Fine 六分类概率聚合得到）
Accuracy: 0.6324
Macro-F1: 0.4595
Weighted-F1: 0.5732
Balanced Accuracy: 0.4974

各类别 Precision / Recall / F1:
  气层优势类: precision=0.6012, recall=0.8254, f1=0.6957
  气水风险类: precision=0.0000, recall=0.0000, f1=0.0000
  非气层类: precision=0.7000, recall=0.6667, f1=0.6829

混淆矩阵:
       气层优势类  气水风险类  非气层类
气层优势类    104      0    22
气水风险类     41      0     2
非气层类      28      0    56

预测标签分布:
class_name  true_count  true_ratio  pred_count  pred_ratio
     气层优势类         126    0.498024         173    0.683794
     气水风险类          43    0.169960           0    0.000000
      非气层类          84    0.332016          80    0.316206

重点类别是否被识别: 否，Recall={'气水风险类': 0.0}

最终说明：fine 六分类结果为主；coarse 三分类和 fine-as-coarse 三分类仅作辅助分析；未用 coarse_pred 硬约束 fine_pred。
评估结果已保存至: C:\Users\18053\Desktop\流体识别\代码\LCRE-CFNet\outputs\LCRE-CFNet_Raw9WithSW_Window0p5m_CoarseFine_Consistency_LAMBDA3.0_C1.0\eval

Process finished with exit code 0

## model

```python
"""LCRE-CFNet coarse-fine model definition."""

from __future__ import annotations

import torch
from torch import nn


class LCRECFNet(nn.Module):
    """Shared DNN backbone with fine and coarse classification heads."""

    def __init__(
        self,
        input_dim: int,
        num_fine_classes: int,
        num_coarse_classes: int,
        dropout: float = 0.5,
    ) -> None:
        super().__init__()
        self.backbone = nn.Sequential(
            nn.Linear(input_dim, 100),
            nn.ReLU(),
            nn.Dropout(dropout),
            nn.Linear(100, 100),
            nn.ReLU(),
            nn.Dropout(dropout),
            nn.Linear(100, 50),
            nn.ReLU(),
            nn.Dropout(dropout),
        )
        self.fine_head = nn.Linear(50, num_fine_classes)
        self.coarse_head = nn.Linear(50, num_coarse_classes)

    def forward(self, x: torch.Tensor) -> tuple[torch.Tensor, torch.Tensor]:
        features = self.backbone(x)
        fine_logits = self.fine_head(features)
        coarse_logits = self.coarse_head(features)
        return fine_logits, coarse_logits
```

## loss

本模型采用多任务联合优化的策略，总损失函数由细分类损失、粗分类损失及一致性约束损失三部分加权构成，具体公式如下：


$$
\text{Total Loss} = \underbrace{\mathcal{L}_{\text{Fine}}}_{\text{细节准确度}} + \underbrace{\lambda_{\text{coarse}} \times \mathcal{L}_{\text{Coarse}}}_{\text{大局观}} + \underbrace{\lambda_{\text{consistency}} \times \mathcal{L}_{\text{Consistency}}}_{\text{逻辑一致性}}
$$


其中各项的具体定义与物理意义如下：

- **$\mathcal{L}_{\text{Fine}}$（细分类损失）**：等于 **交叉熵损失**。
  - 它负责监督模型对 6 种具体岩性/流体（气、水、干层等）的预测能力，是模型的基础判别指标。
- **$\mathcal{L}_{\text{Coarse}}$（粗分类损失）**：等于 **焦点损失**。
  - 它负责监督模型对 3 大类（气层优势、气水风险、非气层）的预测。引入 `Focal Loss` 旨在解决粗分类中样本不均衡的问题，让模型更关注难以识别的“气水风险”类别。
- **$\mathcal{L}_{\text{Consistency}}$（一致性损失）**：等于 **粗细粒度一致性约束**。
  - 它强制要求“细分类的预测结果”在逻辑上必须归属于“粗分类的预测范畴”。例如，如果细分类预测为“气层”，粗分类就不应预测为“水层”。这一项通过惩罚逻辑冲突来规范模型的输出空间。
- **$\lambda_{\text{coarse}}$ 与 $\lambda_{\text{consistency}}$**：等于 **超参数权重**。
  - 分别控制粗分类任务和一致性约束在总损失中的贡献比例，用于平衡不同任务的学习速率。

# 6

## result

E:\Anaconda3\envs\pytorch\python.exe C:\Users\18053\Desktop\流体识别\代码\LCRE-CFNet\eval.py 

Fine 六分类评估（最终任务）
Accuracy: 0.2569
Macro-F1: 0.1532
Weighted-F1: 0.2641
Balanced Accuracy: 0.1503

各类别 Precision / Recall / F1:
  气层: precision=0.3125, recall=0.3390, f1=0.3252
  含气层: precision=0.1304, recall=0.1343, f1=0.1324
  含气水层: precision=0.0000, recall=0.0000, f1=0.0000
  气水同层: precision=0.0000, recall=0.0000, f1=0.0000
  水层: precision=0.0000, recall=0.0000, f1=0.0000
  干层: precision=0.5000, recall=0.4286, f1=0.4615

混淆矩阵:
      气层  含气层  含气水层  气水同层  水层  干层
气层    20   23     9     0   6   1
含气层   16    9     0     0   7  35
含气水层   1    8     0     0   0   0
气水同层  16    1     0     0   0   0
水层     0   17     0     0   0   0
干层    11   11     0     0  26  36

预测标签分布:
class_name  true_count  true_ratio  pred_count  pred_ratio
        气层          59    0.233202          64    0.252964
       含气层          67    0.264822          69    0.272727
      含气水层           9    0.035573           9    0.035573
      气水同层          17    0.067194           0    0.000000
        水层          17    0.067194          39    0.154150
        干层          84    0.332016          72    0.284585

重点类别是否被识别: 否，Recall={'含气水层': 0.0, '气水同层': 0.0, '水层': 0.0}

Coarse 三分类评估（辅助分析）
Accuracy: 0.4032
Macro-F1: 0.3253
Weighted-F1: 0.4106
Balanced Accuracy: 0.3161

各类别 Precision / Recall / F1:
  气层优势类: precision=0.5115, recall=0.5317, f1=0.5214
  气水风险类: precision=0.0000, recall=0.0000, f1=0.0000
  非气层类: precision=0.5000, recall=0.4167, f1=0.4545

混淆矩阵:
       气层优势类  气水风险类  非气层类
气层优势类     67     24    35
气水风险类     43      0     0
非气层类      21     28    35

预测标签分布:
class_name  true_count  true_ratio  pred_count  pred_ratio
     气层优势类         126    0.498024         131    0.517787
     气水风险类          43    0.169960          52    0.205534
      非气层类          84    0.332016          70    0.276680

重点类别是否被识别: 否，Recall={'气水风险类': 0.0}

Fine-as-Coarse 三分类评估（由 Fine 六分类概率聚合得到）
Accuracy: 0.4071
Macro-F1: 0.3256
Weighted-F1: 0.4115
Balanced Accuracy: 0.3188

各类别 Precision / Recall / F1:
  气层优势类: precision=0.5113, recall=0.5397, f1=0.5251
  气水风险类: precision=0.0000, recall=0.0000, f1=0.0000
  非气层类: precision=0.4930, recall=0.4167, f1=0.4516

混淆矩阵:
       气层优势类  气水风险类  非气层类
气层优势类     68     22    36
气水风险类     43      0     0
非气层类      22     27    35

预测标签分布:
class_name  true_count  true_ratio  pred_count  pred_ratio
     气层优势类         126    0.498024         133    0.525692
     气水风险类          43    0.169960          49    0.193676
      非气层类          84    0.332016          71    0.280632

重点类别是否被识别: 否，Recall={'气水风险类': 0.0}

最终说明：fine 六分类结果为主；coarse 三分类和 fine-as-coarse 三分类仅作辅助分析；未用 coarse_pred 硬约束 fine_pred。
评估结果已保存至: C:\Users\18053\Desktop\流体识别\代码\LCRE-CFNet\outputs\LCRE-CFNet_CNN\eval

Process finished with exit code 0

## model 

```python
"""LCRE-CFNet coarse-fine model definition."""

from __future__ import annotations


import torch
import torch.nn as nn


class CNNFeatureExtractor(nn.Module):
    """
    Lightweight CNN feature extractor for windowed well-logging data.

    Input:
        x: [B, L, F]
           B = batch size
           L = window length
           F = number of features per depth point

    Output:
        features: [B, output_dim]
    """

    def __init__(
        self,
        num_features: int,
        output_dim: int = 64,      # 建议先用 64，之后可以试 128
        hidden_dim: int = 32,      # 第一层卷积通道数
        dropout: float = 0.2,
        use_batchnorm: bool = True,
    ) -> None:
        super().__init__()

        block1_layers = [
            nn.Conv1d(
                in_channels=num_features,
                out_channels=hidden_dim,
                kernel_size=3,
                padding=1,
            )
        ]

        if use_batchnorm:
            block1_layers.append(nn.BatchNorm1d(hidden_dim))

        block1_layers.extend([
            nn.ReLU(),
            nn.Dropout(dropout),
        ])

        self.conv_block1 = nn.Sequential(*block1_layers)

        block2_layers = [
            nn.Conv1d(
                in_channels=hidden_dim,
                out_channels=output_dim,
                kernel_size=3,
                padding=1,
            )
        ]

        if use_batchnorm:
            block2_layers.append(nn.BatchNorm1d(output_dim))

        block2_layers.extend([
            nn.ReLU(),
            nn.Dropout(dropout),
        ])

        self.conv_block2 = nn.Sequential(*block2_layers)

        self.pool = nn.AdaptiveAvgPool1d(1)

        self.output_dim = output_dim

    def forward(self, x: torch.Tensor) -> torch.Tensor:
        # x: [B, L, F]
        # Conv1d expects [B, F, L]
        x = x.transpose(1, 2)

        x = self.conv_block1(x)   # [B, hidden_dim, L]
        x = self.conv_block2(x)   # [B, output_dim, L]

        x = self.pool(x)          # [B, output_dim, 1]
        features = x.squeeze(-1)  # [B, output_dim]

        return features

class LCRECFNet(nn.Module):
    """Shared DNN backbone with fine and coarse classification heads."""

    def __init__(
        self,
        num_features: int,
        num_fine_classes: int,
        num_coarse_classes: int,
        cnn_output_dim: int = 64,  # 先用 64，后面试 128
        dropout: float = 0.5,
    ) -> None:
        super().__init__()
        self.cnn = CNNFeatureExtractor(
            num_features=num_features,
            output_dim=cnn_output_dim,
            hidden_dim=32,
            dropout=0.2,
            use_batchnorm=True,
        )
        self.backbone = nn.Sequential(
            nn.Linear(cnn_output_dim, 100),
            nn.ReLU(),
            nn.Dropout(dropout),
            nn.Linear(100, 100),
            nn.ReLU(),
            nn.Dropout(dropout),
            nn.Linear(100, 50),
            nn.ReLU(),
            nn.Dropout(dropout),
        )
        self.fine_head = nn.Linear(50, num_fine_classes)
        self.coarse_head = nn.Linear(50, num_coarse_classes)

    def forward(self, x: torch.Tensor) -> tuple[torch.Tensor, torch.Tensor]:
        cnn_features = self.cnn(x)
        features = self.backbone(cnn_features)
        fine_logits = self.fine_head(features)
        coarse_logits = self.coarse_head(features)
        return fine_logits, coarse_logits
```

