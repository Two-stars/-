## 3. 低对比度气层智能识别

> 对接联系：邓晓红博士

### 信息学院对接联系人

> 刘剑利

---

### （1）背景简介

在实际勘探开发实践中，鄂尔多斯盆地马家沟组白云岩内气层在电测曲线上普遍表现为电阻率升高幅度有限，与同层系含水层之间的电阻率对比度较低，常常难以按照传统“高阻识气”的经验标准进行有效识别。

部分井段在常规测井解释中被归为“低阻或可疑水层”，而录井、试气及生产动态资料表明，该类地层往往能够获得稳定的工业气流，呈现出典型的“低对比度气层”特征。

这导致 Ma5₄¹ 气层的储量和可动程度在早期认识中被系统性低估，对有利区优选和开发层系优化构成制约。

---

### 可供参考的文献

1. Zhang H, Hu Y, Li X, et al. Application of support vector machines and genetic algorithms to fluid identification in Offshore Granitic subduction hill reservoirs[J]. *Geoenergy Science and Engineering*, 2024, 240: 213013.

2. Hua Y, Gao G, He D, et al. Reservoir fluid identification based on multi-head attention with UMAP[J]. *Geoenergy Science and Engineering*, 2024, 238: 212888.

3. Venkateshwaran B, Ramkumar M, Siddiqui N A, et al. A graph convolutional network approach to qualitative classification of hydrocarbon zones using petrophysical properties in well logs[J]. *Natural Resources Research*, 2024, 33(2): 637-664.

---

### （2）数据情况

#### 数据类型

现有 54 口取心井的试气数据和对应的测井曲线数据，包括：

- GR
- AC
- CNL
- DEN
- RT

#### 数据格式

- 流体类型
- 测井数值
- 文件格式：TXT、xlsx

#### 数据量

约 1000 条数据。

---

### （3）需解决的问题

1. **岩性解释与流体识别模型的继承关系**

   如何使岩性解释模型和流体识别模型在技术路线、特征工程或模型结构上具有一定继承性，但又不直接使用相同模型。

2. **数据分布严重不平衡问题**

   数据分布存在明显不平衡，最大类与最小类比例约为 **8.2:1**，即：

   - 含气层
   - 含气水层

   需要在保证准确识别气层等多数类的基础上，进一步提升少数类识别精度。

3. **参数优化、模型选取与应用操作问题**

   其余有关参数优化、模型选取、应用操作等问题，与“复杂碳酸盐岩层系岩性测井识别”中“需解决的问题”相同。

4. **模型方法升级与创新性提升**

   目前采用的机器学习方法主要为基础模型，包括：

   - XGBoost
   - RF
   - SVM
   - BPNN
   - FCNN

   现有方法创新点不足，需要进行适度升级，开展前沿机器学习方法和深度学习方法研究，以提高模型稳定性。

   需要注意避免简单的多模型堆积，应突出方法针对性和实际应用价值。

5. **模型超参数优化**

   需要开展模型超参数优化，使参数能够根据数据特征进行自动调整。

   同时需要给出：

   - 具体优化后的参数
   - 模型训练过程
   - 训练曲线

6. **模型结果评价**

   模型结果应给出训练前后的具体评价数据，包括但不限于：

   - 准确率（Accuracy）
   - 精确率（Precision）
   - 召回率（Recall）
   - F1-score

   也可以采用可编辑图形形式进行展示。

7. **模型检验与测试**

   需要开展跨井验证实验，评估模型在新井中的泛化能力。

8. **模型应用与部署**

   模型应用应做到简单、易操作。

   需要解决以下问题：

   - 如何保存已训练好的模型，便于后续直接调用；
   - 如何避免每次应用时都重新训练模型；
   - 应用数据如何输入更加方便；
   - 模型预测结果如何输出更加高效、清晰。

---

### 测井数据预测与分类技术路线演化

测井数据分类任务主要包括岩性识别、储层类型识别、流体识别、油气水层判别等。其技术路线大致经历了以下演化过程：

```mermaid
flowchart LR
    A[人工经验解释] --> B[交会图/图版法]
    B --> C[统计分析方法]
    C --> D[传统机器学习]
    D --> E[集成学习]
    E --> F[深度学习]
    F --> G[地质约束智能识别]
```



### 初步思路

针对低对比度气层识别任务，核心问题在于数据分布严重不平衡，最大类与最小类比例约为 **8.2:1**，其中主要类别为含气层与含气水层。

研究重点应放在以下方面：

1. 在保证含气层等多数类准确识别的基础上，提升含气水层等少数类的识别精度；
2. 结合测井曲线特征和试气结果，构建适用于低对比度气层的智能识别模型；
3. 通过不平衡学习、模型超参数自动优化和跨井验证，提高模型稳定性与泛化能力；
4. 保存训练完成的模型，形成便于后续批量应用的预测流程，实现输入数据标准化、输出结果自动化。

二分类 + 六分类联合输出



从井级标签分布看，Sheet4 中 78 口井有 77 口井仅对应单一试油结论，唯一多标签井为延1156，包含水层和含气层两个井段。77 口单标签井已覆盖气层、含气层、含气水层、气水同层、水层和干层六类标签，但类别分布高度不均衡，其中气水同层仅 3 口井、含气水层仅 4 口井、水层仅 4 口单标签井。说明本任务的困难不仅来自点级样本不平衡，更来自井级少数类样本稀缺

### 要求分析

在碳酸盐岩储层里，流体识别会受到岩性背景影响。但是目前我们的数据中并没有岩性背景的数据，所以我们先做一个模型，只有那些测定的数据的前提下，我们进行流体识别的分类；然后，我们再加上岩性背景，在进行训练，查看一下我们最后的结果，指标，such as Macro-F1、少数类 Recall、跨井验证结果如果更好，就能说明

> 岩性背景对低对比度气层流体识别有帮助。

那我们目前就分为两个阶段：

#### 阶段一：无岩性输入的基础流体识别模型

我们先只用表中的测井数据（GR、SP、AC、DEN、CNL、PE、RD、RS、POR、SW、PERM 等）来做这个六分类

#### 阶段二：加入岩性识别结果的增强模型

在输入中加入，岩性类别和各岩性的概率，继续进行这个六分类，这样就做到了有继承性但是不是同一个模型。

岩性概率相当于告诉流体模型：

> 当前这个点更接近哪种岩性背景，模型应该在什么背景下理解这些测井响应。	

对于类别不平衡的问题，我认为不应该采取过采样方法：测井数据有地质连续性和物理意义，简单人工合成样本可能产生不合理的测井响应组合。

>针对流体类型样本分布不平衡的问题，应在保持最终六分类目标不变的前提下，采用“粗分类—细分类”的层级识别策略。首先将原始六类流体类型合并为气层优势类、气水风险类和非气层类，建立粗分类模型，使模型优先学习不同流体类型之间的主要判别边界。随后在粗分类结果基础上进一步开展气层、含气层、含气水层、气水同层、水层和干层六分类识别。
>
>同时，在六分类模型训练过程中引入类别权重或 Focal Loss，提高含气水层、气水同层等少数类样本的误分类惩罚，避免模型过度偏向气层、含气层和干层等多数类。模型评价阶段重点采用 Macro-F1、Balanced Accuracy、各类别 Recall 和混淆矩阵，在保证气层、含气层等多数类识别效果稳定的基础上，提升少数类识别精度。







### 流体识别数据分析

#### sheet 2

Sheet2 为原始测井解释数据表，包含井名、深度、常规测井曲线、储层参数、层位、试油结论等信息，可作为低对比度气层智能识别的原始数据基础。

##### 1. 数据基本情况

| 项目     |           结果 |
| -------- | -------------: |
| 样本点数 |           1635 |
| 字段数   |             29 |
| 井数     |             81 |
| 目标字段 |       试油结论 |
| 主要层位 |        马五41a |
| 采样间隔 | 主要为 0.125 m |

主要测井与储层参数包括：

- 常规测井曲线：GR、SP、AC、DEN、CNL、PE、RD、RS；
- 电阻率相关特征：log(RD)、log(RS)、m2r3、m2r6、m2r9；
- 储层参数：POR、SW、PERM；
- 辅助字段：层位、备注；
- 标签字段：试油结论。

---

##### 2. 试油结论类别分布

Sheet2 中共有 6 类试油结论，分布如下：

| 试油结论 | 样本数 |   占比 |
| -------- | -----: | -----: |
| 干层     |    563 | 34.43% |
| 含气层   |    439 | 26.85% |
| 气层     |    437 | 26.73% |
| 水层     |     77 |  4.71% |
| 含气水层 |     76 |  4.65% |
| 气水同层 |     43 |  2.63% |

可以看出，数据存在明显类别不平衡。多数类主要为干层、含气层和气层，少数类主要为水层、含气水层和气水同层。其中最大类“干层”与最小类“气水同层”的样本比例约为 **13.1:1**。

因此，后续模型不能只关注总体准确率，还应重点关注：

- Macro-F1；
- Balanced Accuracy；
- 各类别 Recall；
- 含气水层、气水同层等少数类识别效果；
- 混淆矩阵。

---

##### 3. 层位与备注字段分析

Sheet2 中层位分布较集中：

| 层位    | 样本数 |
| ------- | -----: |
| 马五41a |   1618 |
| 马五41b |     17 |

说明数据主体集中在 **马五41a**，马五41b 样本量很少，暂不适合作为单独层位建模。

备注字段主要包括单采和合采：

| 备注 | 样本数 |
| ---- | -----: |
| 单采 |   1096 |
| 合采 |    539 |

需要注意的是，`备注` 字段与试油结论存在较强关联。例如，干层全部为单采，气水同层全部为合采。因此，`备注` 字段不建议直接作为模型输入，否则模型可能学习到试油方式与标签之间的关系，而不是测井响应与流体类型之间的真实关系。

---

##### 4. 数据质量问题

Sheet2 中存在一定的数据质量问题，主要表现为缺失值和 `-9999` 异常占位值。

###### 4.1 缺失值

| 字段 | 缺失数 |
| ---- | -----: |
| ∆Φ2  |     17 |
| ∆Φ3  |     17 |

该部分缺失集中在同一口井中，说明不是随机缺失，后续需要单独处理。

###### 4.2 `-9999` 异常值

部分字段中存在 `-9999` 异常值，代表无效或缺失数据。

| 字段 | `-9999` 数量 |
| ---- | -----------: |
| SP   |           17 |
| DEN  |           51 |
| DEN1 |           51 |
| CNL  |           17 |
| CNL1 |           17 |
| PE   |           34 |
| m2r6 |          387 |

其中 `m2r6` 的异常值最多，占比约 **23.7%**。虽然 `m2r6` 具有一定地质意义，但由于缺失比例较高，且与其他电阻率曲线存在较强相关性，第一版模型中不建议将其作为核心特征，可在后续消融实验中单独评估其作用。

---

##### 5. 建模启示

基于 Sheet2 的数据分析，可以得到以下认识：

1. **数据可以作为原始建模数据源**  
   Sheet2 包含较完整的测井曲线、储层参数和试油结论信息，具备开展流体类型识别建模的基础。

2. **类别不平衡问题明显**  
   含气水层、气水同层、水层样本量明显少于干层、气层和含气层，后续需要引入类别权重、少数类召回率约束或其他不平衡学习策略。

3. **部分字段需要清洗**  
   `-9999` 应统一转为空值，`∆Φ2`、`∆Φ3` 的集中缺失需要单独处理，`m2r6` 由于缺失比例较高，需谨慎使用。

4. **备注字段存在标签泄漏风险**  
   `备注` 与试油结论关系较强，不建议直接作为模型输入特征。

5. **不能采用简单随机划分**  
   同一井内样本为连续深度点，相关性较强。如果随机划分训练集和测试集，容易造成结果虚高。后续应采用按井分组交叉验证，评估模型在新井中的泛化能力。

6. **需要构造低对比响应增强特征**  
   低对比度气层中，气层与水层电阻率差异不明显，不能只依赖原始电阻率。后续应构造 log 电阻率、深浅电阻率差异、CNL-DEN、AC-DEN、同井相对异常等特征，提高气水差异弱条件下的可分性。

---

##### 6. 小结

总体来看，Sheet2 数据能够支撑低对比度气层六分类识别任务，但在建模前需要进行数据清洗和特征处理。其核心问题包括类别不平衡、异常值、部分字段缺失、井内样本相关性强以及潜在标签泄漏。后续建模应围绕数据清洗、低对比响应增强、类别不平衡优化和跨井验证展开。

#### sheet 4

Sheet4 可以看作是在 Sheet2 基础上进一步筛选后的数据表。相比 Sheet2，Sheet4 删除了部分质量较差的数据，整体更适合作为后续建模主表。

##### 1. 数据基本情况

| 项目     |           结果 |
| -------- | -------------: |
| 样本点数 |           1584 |
| 字段数   |             29 |
| 井数     |             78 |
| 目标字段 |       试油结论 |
| 主要层位 |        马五41a |
| 采样间隔 | 主要为 0.125 m |

相比 Sheet2，Sheet4 少了 **51 条样本、3 口井**。删除的数据主要来自：

| 井名    | 试油结论 | 样本数 |
| ------- | -------- | -----: |
| 靖170-2 | 气层     |     17 |
| 延959-3 | 气层     |     17 |
| 延957-3 | 含气层   |     17 |

这 51 条数据在 Sheet2 中存在较多 `-9999` 异常值，因此 Sheet4 可以认为是经过初步清洗后的数据版本。

---

##### 2. 试油结论类别分布

Sheet4 中仍为 6 类流体识别标签：

| 试油结论 | 样本数 |   占比 |
| -------- | -----: | -----: |
| 干层     |    563 | 35.54% |
| 含气层   |    422 | 26.64% |
| 气层     |    403 | 25.44% |
| 水层     |     77 |  4.86% |
| 含气水层 |     76 |  4.80% |
| 气水同层 |     43 |  2.71% |

可以看出，Sheet4 仍然存在明显类别不平衡。多数类为干层、含气层和气层，少数类为水层、含气水层和气水同层。

其中最大类“干层”与最小类“气水同层”的比例约为：

```text
563 : 43 ≈ 13.1 : 1
```





低对比度气层中，气层电阻率升高幅度有限，且受岩性、孔隙结构、含水性和井间背景差异影响，单纯依赖绝对电阻率阈值容易误判。因此，需要构造同井、同层位和局部背景下的相对响应特征，从“绝对高阻识气”转向“相对响应异常识气”。





> LCRE-CFNet：Low-Contrast Response Enhanced Coarse-to-Fine Network（目前的名字）



| 排名 | 文献                                                         | 与本任务相关性 | 为什么排在这里                                               | 可以借鉴什么                                                 | 不建议直接照搬的地方                                         | 在我们项目中的定位                                           |
| ---- | ------------------------------------------------------------ | -------------- | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 1    | Reservoir fluid type identification method based on deep learning: A case study of the Chang 1 Formation in the Jiyuan oilfield of the Ordos basin, China. | 很高           | 研究对象同样是鄂尔多斯盆地低对比储层流体识别，并且使用深度学习、Focal Loss 和按井划分训练测试，和我们任务最接近。 | 深度学习建模思路；Focal Loss 处理类别不平衡；按单井划分训练/测试；与 LR、NB、GBDT、RF、SVM 等模型对比。 | 具体层位和数据不同，不能直接套用其模型结构和参数。           | 最核心参考，支撑我们“深度学习 + 不平衡损失 + 跨井验证”的主方案。 |
| 2    | Reservoir Identification from Well-Logging Data Using a Focal Loss-Enhanced Convolutional Neural Network: A Case Study from the Chang 8 Formation, Ordos Basin. Processes, 2026. | 高             | 虽然任务是储层识别，不完全是流体六分类，但它使用 CNN + Focal Loss，并强调按井划分评估泛化能力，和我们的模型设计高度相似。 | CNN/1D-CNN 作为测井深度学习 baseline；Focal Loss 缓解类别不平衡；按井验证；与 RF、CNN-LSTM 等模型对比。 | 任务不是严格的流体六分类，不能直接作为流体识别模型照搬。     | 支撑我们设计“轻量深度学习 + 类别不平衡损失 + 跨井验证”。     |
| 3    | Hua Y, Gao G, He D, et al. Reservoir fluid identification based on multi-head attention with UMAP. Geoenergy Science and Engineering, 2024. | 高             | 这是甲方给的参考文献之一，直接做储层流体识别，并使用 UMAP 和多头注意力，能支撑我们引入轻量注意力或深度学习升级。 | UMAP 可用于类别可分性可视化；注意力机制可用于多测井曲线关系建模；可借鉴“多曲线融合 + 深度学习”的表述。 | 多头注意力/Transformer 对当前小样本数据可能偏复杂，第一版不建议直接作为主模型。 | 作为深度学习升级和注意力模块的主要参考。                     |
| 4    | Gao L, Xie R H, Xiao L Z, et al. Identification of low-resistivity-low-contrast pay zones in the feature space with a multi-layer perceptron based on conventional well log data. Petroleum Science, 2022. | 高             | 明确研究低阻低对比产层，强调低对比层容易因与水层电阻率相似而被忽略，非常支撑我们“相对响应异常识别”的思想。 | “低对比是相对概念”；特征空间构建；DBSCAN/特征重构思想；MLP 作为深度学习基线。 | 研究对象是低阻低对比产层，不完全等同于马家沟组低对比气层六分类。 | 支撑我们提出“从绝对高阻识气转向相对响应异常识气”。           |
| 5    | Bai Z, Tan M, Shi Y, et al. Log interpretation method of resistivity low-contrast oil pays in Chang 8 tight sandstone of Huanxian area, Ordos Basin by support vector machine. Scientific Reports, 2022. | 较高           | 同样是鄂尔多斯盆地低电阻率对比油层识别，问题背景和“低对比”非常接近，但方法偏传统 SVM。 | 低对比储层背景论证；传统高阻识别不足；SVM 作为经典低对比流体识别基线；与交会图、BP、RBF 等方法对比。 | 研究对象是低对比油层，不是气层；SVM 适合作为基线，不适合作为主创新方法。 | 作为低对比储层机器学习识别的传统基线参考。                   |
| 6    | Zhang H, Hu Y, Li X, et al. Application of support vector machines and genetic algorithms to fluid identification in Offshore Granitic subduction hill reservoirs. Geoenergy Science and Engineering, 2024. | 中等           | 这是甲方给的参考文献之一，直接做流体识别，但研究对象是海上花岗岩潜山储层，地质背景和我们不同；方法是 GA-SVM，更偏传统机器学习。 | SVM 流体识别；遗传算法自动优化参数；敏感参数组合；可作为传统机器学习和自动调参参考。 | 仍属于传统机器学习，创新性不够；不适合作为我们主模型。       | 作为 SVM/GA-SVM 传统基线和自动调参参考。                     |
| 7    | Reservoir Fluid Identification Based on Bayesian-Optimized SVM Model. Processes, 2025. | 中等           | 与流体识别和低对比储层有关，但核心仍是 BO-SVM，更多是参数优化和传统模型改进。 | 敏感测井参数筛选；贝叶斯优化超参数；SVM 作为可解释传统模型。 | 方法偏传统，不满足甲方“适度前沿深度学习”的倾向。             | 作为超参数优化和传统基线补充参考。                           |
| 8    | Venkateshwaran B, Ramkumar M, Siddiqui N A, et al. A graph convolutional network approach to qualitative classification of hydrocarbon zones using petrophysical properties in well logs. Natural Resources Research, 2024. | 中等偏低       | 这是甲方给的参考文献之一，属于前沿图神经网络方法，但任务是含烃区定性分类，不是直接的低对比气层六分类。 | 岩石物理参数融合；深度/邻接关系建模；GCN 作为前沿方法储备。  | GCN 需要构建图结构，工程复杂度高，当前数据量和任务形态下不建议作为主路线。 | 作为前沿方法参考，不作为当前主模型。                         |



从相关性排序看，最值得重点参考的是低对比储层流体识别、Focal Loss 深度学习、CNN/1D-CNN 及按井验证相关论文。这些文献能够直接支撑本项目的核心设计：轻量深度学习 baseline、低对比响应增强、类别不平衡损失和跨井验证。甲方给出的三篇文献中，Hua et al. 的 UMAP + 多头注意力与我们深度学习升级方向最相关；Zhang et al. 的 GA-SVM 更适合作为传统基线和参数优化参考；Venkateshwaran et al. 的 GCN 方法具有前沿性，但工程复杂度较高，暂不建议作为当前主路线。



##### 3. 列名解析

| 字段      | 含义                               | 主要反映什么                                                 |
| --------- | ---------------------------------- | ------------------------------------------------------------ |
| 井名      | 井的名称或编号                     | 用于区分不同井                                               |
| 深度      | 测井采样点对应的深度               | 表示该行数据所在井深位置                                     |
| GR        | 自然伽马                           | 主要反映泥质含量、岩性变化                                   |
| SP        | 自然电位                           | 反映渗透性、地层水矿化度、泥岩基线变化等                     |
| AC        | 声波时差                           | 反映孔隙度、岩性、压实程度，气层中也可能有响应               |
| AC1       | 声波时差相关派生/校正字段          | 可能是 AC 的校正值、归一化值或解释处理值                     |
| CAL       | 井径                               | 反映井眼扩大、井壁稳定性，也能提示测井质量                   |
| U         | 不确定，可能为铀含量或某个解释参数 | 若为铀含量，常用于辅助判断泥质/放射性矿物；若为自定义参数，需要确认 |
| DEN       | 密度测井                           | 反映岩石体积密度，与孔隙度、岩性有关                         |
| DEN1      | 密度相关派生/校正字段              | 可能是 DEN 的校正值、归一化值或解释处理值                    |
| CNL       | 补偿中子测井                       | 反映氢指数，对孔隙流体和气层响应敏感                         |
| CNL1      | 中子相关派生/校正字段              | 可能是 CNL 的校正值、归一化值或解释处理值                    |
| PE        | 光电吸收截面指数                   | 对岩性、矿物成分敏感，尤其适合碳酸盐岩岩性背景判断           |
| RD        | 深电阻率                           | 反映较深探测范围内的地层电阻率，是流体识别核心曲线之一       |
| RS        | 浅电阻率                           | 反映浅部地层电阻率，受泥浆侵入影响更明显                     |
| log（RD） | RD 的对数值                        | 减弱电阻率量纲差异，增强稳定性                               |
| log（RS） | RS 的对数值                        | 减弱浅电阻率量纲差异                                         |
| m2r3      | 阵列感应/多探测深度电阻率相关曲线  | 可能代表某一探测深度或通道的电阻率响应                       |
| m2r6      | 阵列感应/多探测深度电阻率相关曲线  | 可能代表中等探测深度电阻率响应                               |
| m2r9      | 阵列感应/多探测深度电阻率相关曲线  | 可能代表较深探测深度或另一通道电阻率响应                     |
| POR       | 孔隙度                             | 反映储层储集空间大小                                         |
| SW        | 含水饱和度                         | 反映孔隙中水的占比，与气/水识别直接相关                      |
| PERM      | 渗透率                             | 反映流体流动能力                                             |
| 试油结论  | 试油/解释得到的流体类型结果        | 本任务的分类标签                                             |
| 层位      | 地层层位，如马五41a                | 表示样本所属地层单元                                         |
| ∆Φ1       | 孔隙响应差异类派生特征             | 可能表示不同孔隙度曲线之间的差异                             |
| ∆Φ2       | 孔隙响应差异类派生特征             | 可能是公式计算字段                                           |
| ∆Φ3       | 孔隙响应差异类派生特征             | 可能是公式计算字段                                           |
| 备注      | 单采/合采等试油方式备注            | 与试油解释流程有关                                           |



| 类型         | 字段                                       | 作用                                     |
| ------------ | ------------------------------------------ | ---------------------------------------- |
| 标识字段     | 井名、深度                                 | 用于分组、排序、构造窗口，不直接作为输入 |
| 常规测井曲线 | GR、SP、AC、CAL、DEN、CNL、PE              | 描述岩性、孔隙结构、井眼环境             |
| 电阻率曲线   | RD、RS、log(RD)、log(RS)、m2r3、m2r6、m2r9 | 描述流体和地层电性响应                   |
| 储层参数     | POR、SW、PERM                              | 描述孔隙度、含水性、渗透能力             |
| 派生特征     | AC1、DEN1、CNL1、∆Φ1、∆Φ2、∆Φ3             | 可能是校正值、归一化值或孔隙响应组合     |
| 标签字段     | 试油结论                                   | 六分类目标                               |
| 辅助解释字段 | 层位、备注                                 | 层位可辅助理解，备注不建议进模型         |

### 初始输入数据选择

#### 1. 特征选择的基本思路

在建模前，我没有直接把 Sheet1 中所有字段全部输入模型，而是先做了一轮前置分析，主要包括：

* 测井物理意义初选；
* 箱线图和直方图分析特征分布；
* Pearson 相关矩阵分析特征冗余；
* 互信息分析特征与六分类标签之间的关系；
* 最终确定第一版输入特征。

这样做的目的是避免直接使用大量冗余字段、异常字段或可能存在标签泄漏风险的字段，使第一版模型输入更加简洁、稳定、可解释。

---

2. 第一版最终输入特征

综合前置分析后，第一版模型输入特征确定为：

```text
GR、SP、AC、CAL、DEN、CNL、PE、RD、SW、RD_RS_ratio
```

其中：

```text
RD_RS_ratio = RD / RS
```

这 10 个特征分别覆盖了岩性背景、井眼环境、孔隙结构、电性响应、含水性和深浅电阻率相对差异。

| 特征        | 主要意义                                   |
| ----------- | ------------------------------------------ |
| GR          | 反映泥质含量和岩性背景                     |
| SP          | 反映地层水性质、渗透性和电化学响应         |
| AC          | 反映孔隙结构和声波响应                     |
| CAL         | 反映井眼环境和测井质量                     |
| DEN         | 反映岩性、密度和孔隙结构                   |
| CNL         | 反映氢指数和孔隙流体响应                   |
| PE          | 反映矿物组成，对碳酸盐岩背景有帮助         |
| RD          | 反映地层深部电阻率和流体响应               |
| SW          | 反映含水饱和度，与气水识别直接相关         |
| RD_RS_ratio | 反映深浅电阻率相对差异，增强低对比识别能力 |

---

#### 3. 箱线图和直方图分析结论

箱线图结果显示，不同试油结论类别在 GR、SP、AC、CNL、RD、SW、RD_RS_ratio 等指标上存在一定分布差异，说明这些特征中确实包含与流体识别相关的信息。

但同时，各类别在单一测井曲线上的重叠也比较明显，说明不能依靠单个指标直接完成六分类。例如，不能简单认为“RD 高就是气层”，也不能单独依靠 SW 或 SP 判断流体类型。因此，本任务更适合采用多参数融合模型进行综合识别。

直方图结果显示，部分特征存在偏态和长尾现象，尤其是 RD 这类电阻率指标。RD 的大部分样本集中在较低区间，但右侧存在少量极高值，说明后续模型训练前需要进行标准化处理；如果后续发现极端值影响明显，可以再对 RD 尝试 log 变换、RobustScaler 或分位数截尾等处理方式。

---

#### 4. Pearson 相关矩阵分析结论

全量 Pearson 相关矩阵显示，原始字段中存在较多冗余关系。例如：

* AC 与 AC1 高度相关；
* DEN 与 DEN1 高度相关；
* CNL 与 CNL1 高度相关；
* RD、RS、log(RD)、log(RS) 及部分电阻率派生特征高度相关；
* ∆Φ1、logRD_logRS_diff、RD_RS_ratio 表达的信息比较接近。

因此，第一版模型没有直接输入全部字段，而是保留代表性更强、物理意义更清楚、冗余相对较低的核心特征。

在精简输入特征的 Pearson 矩阵中，大部分特征之间相关性较低或中等，说明第一版输入组合整体冗余较少。对于 RD 和 RS 相关性较高的问题，第一版没有直接输入 RS，而是采用：

```text
RD + RD_RS_ratio
```

其中，RD 表征电阻率绝对响应，RD_RS_ratio 表征深浅电阻率相对差异。这样既保留了浅电阻率的信息，又减少了 RD 和 RS 同时输入造成的冗余。

---

#### 5. 互信息分析结论

互信息用于衡量每个特征与六分类标签之间的信息关系。互信息越高，说明该特征对判断试油结论类别越有帮助。

第一版输入特征的互信息结果如下：

| 排名 | 特征        | 互信息 |
| ---: | ----------- | -----: |
|    1 | SP          |  0.746 |
|    2 | RD_RS_ratio |  0.457 |
|    3 | CAL         |  0.216 |
|    4 | PE          |  0.192 |
|    5 | CNL         |  0.167 |
|    6 | DEN         |  0.146 |
|    7 | SW          |  0.142 |
|    8 | GR          |  0.131 |
|    9 | AC          |  0.111 |
|   10 | RD          |  0.106 |

结果表明，第一版 10 个输入特征均与六分类标签存在一定信息关系。其中，SP 的互信息最高，说明自然电位对流体类别识别具有较强贡献；RD_RS_ratio 排名第二，说明深浅电阻率相对差异对低对比气层识别是有意义的。

候选特征中，m2r9_m2r3_ratio、PERM、POR、AC1 等也具有一定潜力，但它们要么属于派生特征，要么属于解释参数，要么与已有曲线存在较强相关性。因此第一版先不直接加入，而是放到后续消融实验中逐步验证。

---

#### 6. 暂不输入的字段及原因

| 字段                        | 暂不输入原因                                      |
| --------------------------- | ------------------------------------------------- |
| 井名                        | 只用于按井分组验证，不作为模型输入                |
| 深度                        | 第一版不直接输入，后续可用于构造深度窗口          |
| 试油结论                    | 标签字段，不能作为输入                            |
| 层位                        | 当前信息量有限，第一版暂不使用                    |
| 备注                        | 可能包含与试油结论相关的信息，存在标签泄漏风险    |
| RS                          | 与 RD 高度相关，其相对信息已通过 RD_RS_ratio 保留 |
| AC1、DEN1、CNL1             | 与 AC、DEN、CNL 信息重复，后续再做消融实验        |
| log(RD)、log(RS)            | 与 RD、RS 高度相关，第一版暂不输入                |
| ∆Φ1、logRD_logRS_diff       | 与 RD_RS_ratio 信息接近，第一版只保留 RD_RS_ratio |
| m2r3、m2r9、m2r9_m2r3_ratio | 有潜力，但作为后续扩展特征验证                    |
| POR、PERM                   | 与标签相关，但属于解释参数，第一版先不依赖        |
| m2r6                        | 缺失/异常值较多                                   |
| ∆Φ2、∆Φ3                    | 存在 `#REF!` 公式错误                             |

---

#### 7. Z-score 标准化处理

确定第一版输入特征后，模型训练前需要对这些连续特征进行 Z-score 标准化。

原因是不同测井指标的量纲和数值范围差异较大。例如，AC 的数值可能在几百左右，DEN 只有 2 左右，RD 可能从几十到上千，SW 接近 0–100，而 RD_RS_ratio 通常集中在 1 附近。如果直接输入神经网络，数值范围较大的特征可能在训练中占据更大影响，导致模型训练不稳定。

Z-score 标准化公式为：

```text
X_std = (X - μ) / σ
```

其中，μ 为均值，σ 为标准差。标准化后，各特征会被转换到均值约为 0、标准差约为 1 的尺度空间，有助于提升模型训练稳定性。

需要注意的是，标准化必须避免数据泄露。正确做法是：

```text
按井划分训练集 / 验证集 / 测试集
→ 只用训练集计算均值和标准差
→ 用训练集得到的均值和标准差处理训练集、验证集和测试集
```

也就是说，标准化器只能在训练集上拟合，验证集和测试集只能使用训练集得到的参数进行转换。这样可以避免测试井的信息提前参与训练过程，保证跨井验证结果更真实。

---

#### 8. 小结

最终，第一版模型输入特征确定为：

```text
GR、SP、AC、CAL、DEN、CNL、PE、RD、SW、RD_RS_ratio
```

这组特征不是随意选择的，而是结合测井物理意义、箱线图/直方图分布、Pearson 相关矩阵和互信息分析后确定的。它既覆盖了低对比度气层识别所需的主要测井响应信息，又避免了大量冗余字段和异常字段进入模型。

后续如果第一版模型效果不足，可以通过消融实验逐步加入 m2r9_m2r3_ratio、POR、PERM、AC1、DEN1、CNL1 等候选特征，验证其是否能进一步提升模型性能。



### 补充特征（从其他文献中找到的黄金特征）

针对鄂尔多斯盆地马家沟组白云岩储层“低阻、低对比度”的地质特征，为有效区分气层、含气水层、气水同层及水层，构建了以下五大核心物理特征。这些特征旨在消除岩性、物性干扰，精准捕捉流体性质与可动性。

#### 1. 视地层水电阻率 ($R_{wa}$) 

- **计算公式**：$R_{wa} = \frac{R_t \cdot \phi^m}{a}$（砂岩/白云岩经验值通常取 $a=1, m=2$，具体需根据阿尔奇公式参数标定）。 

- **物理意义**：$R_{wa}$ 是剥离了孔隙度影响后的“标准化电阻率”。
-  **实战价值**：作为应对低阻气层的“照妖镜”，它能消除物性对电阻率的干扰。在低对比度气层中，即使绝对电阻率（$R_t$）不高，只要 $R_{wa}$ 明显脱离水层基线向上抬升，即指示地层具有含气性。 

#### 2. 泥质含量 ($V_{sh}$) 

- **计算公式**：基于自然伽马（GR）或自然电位（SP）的非线性指数模型计算（如 $V_{sh} = \frac{2^{GCUR \cdot IGR} - 1}{2^{GCUR} - 1}$）。 

- **物理意义**：反映岩石中粘土矿物的相对含量。 

- **实战价值**：作为排除岩性导电干扰的“过滤器”。低阻现象常由岩石中的分散泥质或导电矿物引起。$V_{sh}$ 能辅助模型识别“因泥质含量高导致的低阻”，从而避免将泥质引起的低阻气层误判为水层。 

#### 3. 孔隙度 ($\phi$) 

- **计算公式**：利用声波时差（AC）、密度（DEN）等曲线通过威利公式或密度孔隙度公式计算得出。
-  **物理意义**：量化储层的储集空间大小。
-  **实战价值**：作为衡量储集能力的“标尺”。它能直接帮助模型划清“有效储层”与“致密干层”的界限，区分具有储集能力的低阻层（如水层、含气水层）与无产能的致密层（干层）。 

#### 4. 挖掘效应差值 (Excavation Effect Difference) 

- **计算公式**：`Excavation_Diff = Phi_density - Phi_neutron`（密度孔隙度 - 中子孔隙度）。
- **物理意义**：利用天然气“密度极低”与“含氢指数极低”的物理特性差异。
- **实战价值**：识别天然气的“黄金指标”。气层在测井曲线上会呈现“挖掘效应”（密度孔隙度偏大，中子孔隙度显著偏低）。即使电阻率特征不明显，只要该差值明显为正（呈“剪刀差”），即强烈暗示地层中含有天然气，是区分气水同层与纯水层的关键。

####  5. 侵入特征 (Invasion Characteristic) 

- **计算公式**：`Invasion_Diff = R_deep - R_shallow`（深探测电阻率 - 浅探测电阻率）。 
- **物理意义**：反映钻井液滤液侵入地层后，冲洗带与原状地层的电阻率径向差异。 
- **实战价值**：反映流体可动性的“探针”。气层/油层因油气不易被驱替，深、浅电阻率常呈现明显的正向幅度差；而水层因流体易被置换，深浅电阻率基本重合。该特征能有效捕捉流体的可动性，辅助区分含气水层与水层。 

**总结**：这五个特征涵盖了**岩性（$V_{sh}$）、物性（$\phi$）、含气性（$R_{wa}$、挖掘效应）以及流体可动性（侵入特征）**四个核心维度，能够为机器学习模型提供扎实的物理先验知识。



**LCRE-CFNet**

Low-Contrast Response Enhanced Coarse-to-Fine Network

低对比响应增强粗细联合识别网络
